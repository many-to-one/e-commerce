# stop all active container:
docker stop $(docker ps -q)

# Clear build cache and rebuild:
docker builder prune -af # will delete containers cash
docker-compose build --no-cache


# Delete container logs:
docker logs NAZWA_KONTENERA --tail 0 > /dev/null

# Limit logs size in .yml
logging:
  driver: "json-file"
  options:
    max-size: "50m"
    max-file: "3"
