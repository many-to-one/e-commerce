# Permissions deny to edit files:
ls -l (check permissions)
sudo chmod -R 777 /path/to/file or directory or . (777 - read, wtite, execute)

# ngnix
cd /etc/nginx/sites-available/

sudo ln -s /etc/nginx/sites-available/yourdomain.conf /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx

# Chek your vps CPU time still and other parameters
mpstat -P ALL 1

# Ilość CPU
lscpu lub nproc


# delete folder with files:
sudo rm -rf ./server/staticfiles

# copy folder with files to the path (exp):
sudo cp -r ./server/fonts ./server/staticfiles

### GIT ###
If you don't need git add .
make git reset (before git pull)
